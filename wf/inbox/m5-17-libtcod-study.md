# 任務書 M5.17 — libtcod 能不能直接套？（**只產出判斷，不寫程式碼**）

**寄件人**：Opus 5 規劃者
**收件人**：**gpt-sol 實作者**
**本輪只寫一份回報。不要改任何 `core/`、`data/`、`tests/`、`design/`、`cmake/`。**
（M5.15 在做歸約、M5.16 在調 FOV 預算。）

---

## 使用者的提議

> **「其實直接去套 libtcod 也可以。去看 `~/repo/game_dev` 和 `~/repo/pas`。」**

⚠ **使用者補充：`~/repo/pas/analysis/` 與 `~/repo/game_dev/analysis/` 才是該看的**——
那裡是消化過的分析，比啃原始碼快。底下與本題相關的至少有
`brogue-ce`、`dcss`、`cdda`、`luanti`、`generator`。**先掃分析目錄，
原始碼只在分析講不清楚時才回頭翻。**

libtcod 本身在 `~/repo/game_dev/python-tcod`（Python 綁定，含 C 端來源）。

## ⚠ 必須先回答的那一題

> **libtcod 的 FOV／尋路是「格」為單位的嗎？**

我們的牆與門是**邊屬性**（`EdgeDef`，與 L1 河流道路、L2 城牆共用同一套機制）。
M5.14 整輪的重點就是**擋視線必須查邊，不能查格**——而查格的版本在多數地圖上
結果差不多，**是假通過的教科書案例**（負向控制一改就紅）。

所以：

1. **libtcod 的 FOV 演算法怎麼判斷遮蔽？**貼出實際的判斷程式碼與**檔案路徑加行號**。
2. 如果是查格，**我們的邊屬性牆有沒有辦法表達？** 老實回答。
   把每道牆膨脹成一格會**浪費一半地圖**，那是設計文件明確拒絕過的表達方式。
3. **有沒有折衷？**（例如只借 libtcod 的某些部分、或用兩倍解析度的影子網格）
   如果折衷的代價比自己寫還高，**就說代價比自己寫還高**。

## 其他值得評估的部分

libtcod 不只有 FOV。逐項給**值不值得借**的判斷：

| 部分 | 我們現在 | 值不值得 |
|---|---|---|
| FOV | M5.14 的整數 DDA 查邊 | ？ |
| 尋路（A\*／Dijkstra map） | Site 有 A\*，Local 還沒有 | ？ |
| BSP 分割 | 我們有 `core/spatial/recursive_partition`（Site／Local 共用） | ？ |
| 噪聲 | `gen_noise.h` 的 fbm | ？ |
| 隨機數 | 自己的 splitmix64 | ？ |
| 場景圖／渲染／輸入 | **完全不需要**（顯示在 Godot） | ？ |

⚠ **決定性是硬條件**：任何借進來的東西必須**同 seed 同輸出、跨平台一致**。
libtcod 的 RNG 或浮點路徑若做不到，那一項就直接淘汰——**這條不能妥協**。

⚠ **`aetheria_core` 的依賴邊界**：現在只有 EnTT／cereal／toml++／zstd。
多一個依賴要講清楚換到什麼。

## 另外：別人怎麼表達牆

從 `~/repo/pas/analysis/` 的 `brogue-ce`、`dcss`、`cdda` 找——
**有沒有人用邊屬性表達牆？** 那是我們最特殊的一點。
⚠ **如果大家都用格，值得知道原因**：是因為邊比較難寫，還是因為格在玩法上其實夠用？
老實回答，不要為了配合我們現有的作法而美化。

## 回報

`wf/inbox/m5-17-libtcod-study-complete.md`（≤ 8 KB，超過就依子題拆檔）：

1. 上面那題的直接答案
2. 逐項的借／不借與理由
3. **一句話結論：該不該套，還是維持自己寫**

⚠ 我要**排序過的建議**，不是「全都可以考慮」。

## 規約

- 不改任何程式碼｜不准 fan-out 子 agent｜不要改 `design/`｜不要 push｜繁中
- 引用外部一律附**檔案路徑與行號**
